# Data Pipeline 项目总览

## 📋 项目简介

这是一个功能完整的**自动化数据采集与 RAG 库构建系统**，与 `backend-ai` 项目配合使用，为行业分析 AI 助手提供持续更新的知识库。

## 🎯 核心目标

1. **自动化数据采集**：从多个数据源自动获取行业报告和文档
2. **智能数据处理**：文本提取、清洗、元数据提取
3. **向量化存储**：将文档分块、嵌入并存储到 Pinecone
4. **定时调度**：按计划自动执行采集任务
5. **与 backend-ai 集成**：共享 Pinecone 数据库，无缝对接

## 🏗️ 架构设计

```
┌─────────────────────────────────────────────────────────┐
│                   Data Pipeline                         │
│                                                          │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │ API Clients  │  │   Scrapers   │  │  Scheduler   │  │
│  │              │  │              │  │              │  │
│  │ • SEC EDGAR  │  │ • McKinsey   │  │ • Cron Jobs  │  │
│  │ • NewsAPI    │  │ • BCG        │  │ • Task Queue │  │
│  │ • World Bank │  │ • News Sites │  │              │  │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘  │
│         │                 │                 │           │
│         └─────────────────┴─────────────────┘           │
│                           │                             │
│                           ↓                             │
│                  ┌─────────────────┐                    │
│                  │   Processors    │                    │
│                  │                 │                    │
│                  │ • PDF Extract   │                    │
│                  │ • Text Clean    │                    │
│                  │ • Metadata      │                    │
│                  └────────┬────────┘                    │
│                           │                             │
│                           ↓                             │
│                  ┌─────────────────┐                    │
│                  │    Ingest       │                    │
│                  │                 │                    │
│                  │ • Chunk Text    │                    │
│                  │ • Generate      │                    │
│                  │   Embeddings    │                    │
│                  │ • Upsert        │                    │
│                  └────────┬────────┘                    │
└──────────────────────────┼──────────────────────────────┘
                           │
                           ↓
                  ┌─────────────────┐
                  │    Pinecone     │
                  │ Vector Database │
                  │   (Shared)      │
                  └────────┬────────┘
                           │
                           ↓
                  ┌─────────────────┐
                  │   backend-ai    │
                  │   RAG Service   │
                  └─────────────────┘
```

## 📁 项目结构详解

```
data-pipeline/
│
├── config/                      # 配置管理
│   ├── settings.py             # 环境变量和设置
│   └── sources.yaml            # 数据源配置
│
├── api_clients/                # API 客户端模块
│   ├── sec_client.py           # SEC EDGAR API
│   │   - 获取公司财报（10-K, 10-Q）
│   │   - Ticker 到 CIK 映射
│   │   - 批量下载
│   ├── newsapi_client.py       # NewsAPI
│   │   - 行业新闻搜索
│   │   - 头条新闻
│   └── worldbank_client.py     # 世界银行 API（待实现）
│
├── scrapers/                   # 网络爬虫模块
│   ├── base_scraper.py         # 爬虫基类
│   │   - HTTP 请求管理
│   │   - 频率限制
│   │   - HTML 解析
│   │   - 文件下载
│   ├── mckinsey_scraper.py     # McKinsey 爬虫
│   │   - 文章列表爬取
│   │   - 内容提取
│   │   - PDF 下载
│   └── news_scraper.py         # 通用新闻爬虫（待实现）
│
├── processors/                 # 数据处理模块
│   ├── pdf_processor.py        # PDF 处理
│   │   - 文本提取（PyMuPDF）
│   │   - 元数据提取
│   │   - 有效性验证
│   ├── text_cleaner.py         # 文本清洗
│   │   - 移除噪声
│   │   - 标准化格式
│   │   - 句子分割
│   └── metadata_extractor.py  # 元数据提取
│       - 从文件名提取
│       - 从内容推断
│       - 行业分类
│
├── ingest/                     # 数据摄取模块
│   ├── pinecone_ingester.py   # Pinecone 摄取器
│   │   - 单文档摄取
│   │   - 批量摄取
│   │   - 摄取验证
│   └── batch_processor.py      # 批量处理器
│       - 目录扫描
│       - 端到端处理流程
│       - 错误处理
│
├── scheduler/                  # 任务调度模块
│   └── task_scheduler.py       # 调度器
│       - 定时任务配置
│       - 任务执行管理
│       - 执行历史记录
│
├── utils/                      # 工具模块
│   ├── logger.py              # 日志配置
│   ├── rate_limiter.py        # 频率限制器
│   └── file_utils.py          # 文件工具
│
├── storage/                    # 本地存储
│   ├── raw/                   # 原始下载文件
│   │   ├── sec_filings/
│   │   ├── mckinsey_reports/
│   │   └── news_articles/
│   ├── processed/             # 处理后的数据
│   └── logs/                  # 日志文件
│
├── tests/                      # 测试
│   └── test_processors.py
│
├── main.py                     # 主入口（调度器）
├── run_scrapers.py            # 手动运行爬虫
├── run_batch_ingest.py        # 手动批量摄取
├── requirements.txt           # Python 依赖
├── .env.example               # 环境变量示例
├── .gitignore                 # Git 忽略规则
│
├── README.md                   # 完整文档
├── QUICKSTART.md              # 快速开始
└── PROJECT_OVERVIEW.md        # 本文件
```

## 🔄 数据流程

### 1. 数据采集阶段

```
API/网页 → 下载 → 保存到 storage/raw/
```

**实现模块**：
- `api_clients/` - API 数据获取
- `scrapers/` - 网页爬取

### 2. 数据处理阶段

```
原始文件 → PDF提取 → 文本清洗 → 元数据提取
```

**实现模块**：
- `processors/pdf_processor.py`
- `processors/text_cleaner.py`
- `processors/metadata_extractor.py`

### 3. 数据摄取阶段

```
清洗后文本 → 分块 → 生成嵌入 → 上传 Pinecone
```

**实现模块**：
- `ingest/pinecone_ingester.py`
- 复用 `backend-ai` 的 RAGService

### 4. 自动化调度

```
Cron Schedule → 执行任务 → 记录日志 → 清理旧文件
```

**实现模块**：
- `scheduler/task_scheduler.py`

## 🔑 核心组件说明

### SECClient (api_clients/sec_client.py)

**功能**：
- 从 SEC EDGAR 获取公司财报
- 支持 10-K（年报）和 10-Q（季报）
- 自动处理 Ticker → CIK 映射
- 遵守 SEC 的频率限制（10 req/s）

**使用示例**：
```python
client = SECClient()
filings = client.get_company_filings("AAPL", ["10-K"], limit=5)
client.bulk_download_filings(["AAPL", "MSFT"], output_dir=Path("output"))
```

### McKinseyScraper (scrapers/mckinsey_scraper.py)

**功能**：
- 爬取 McKinsey Insights 网站
- 提取文章标题、内容、作者
- 下载 PDF 报告
- 礼貌性爬取（5秒延迟）

**使用示例**：
```python
scraper = McKinseyScraper()
results = scraper.scrape(max_items=10)
scraper.save_results(results, output_file)
```

### BatchProcessor (ingest/batch_processor.py)

**功能**：
- 批量处理 PDF 文件
- 自动提取和清洗
- 智能元数据提取
- 行业自动检测

**使用示例**：
```python
processor = BatchProcessor()
result = processor.process_directory(
    directory=Path("storage/raw/sec_filings"),
    industry="Technology"
)
```

### TaskScheduler (scheduler/task_scheduler.py)

**功能**：
- 根据配置文件设置定时任务
- 每日/每周任务支持
- 任务执行历史记录
- 错误处理和重试

**使用示例**：
```python
scheduler = TaskScheduler()
scheduler.run_forever()  # 持续运行
# 或
scheduler.run_once('sec')  # 运行单次
```

## 🔧 配置说明

### 环境变量 (.env)

```env
# 必需
PINECONE_API_KEY=xxx
GOOGLE_API_KEY=xxx

# 可选但推荐
NEWSAPI_KEY=xxx
SEC_USER_AGENT=Your Name your@email.com
```

### 数据源配置 (config/sources.yaml)

```yaml
data_sources:
  sec:
    enabled: true          # 是否启用
    frequency: daily       # 运行频率
    config:
      companies: [AAPL, MSFT]
      max_per_company: 5

schedule:
  daily_tasks:
    - source: sec
      time: "02:00"        # 每天凌晨2点运行
```

## 📊 与 backend-ai 的集成

### 共享的内容

1. **Pinecone 向量数据库**
   - 使用相同的 Index
   - 相同的嵌入模型（Gemini）
   - 相同的元数据格式

2. **数据格式约定**
   ```python
   metadata = {
       "file_id": str,
       "industry": str,
       "year": str,
       "source": "automated_collection",  # 标识为自动采集
       "chunk_index": int,
       "text": str
   }
   ```

3. **可复用的代码**
   - `RAGService` - 摄取逻辑
   - `PDFParser` - PDF 处理

### 工作流程

```
data-pipeline           backend-ai
     │                      │
     │ 1. 采集数据          │
     ↓                      │
  存储到本地                │
     │                      │
     │ 2. 处理并摄取        │
     ↓                      │
  Pinecone Index  ←─────── │ 3. 查询检索
     │                      │
     │                      ↓
     └──────────────→  返回用户答案
```

## 🚀 部署建议

### 开发环境

```bash
# 本地运行
python main.py --mode once --task sec

# 测试单个组件
python -c "from api_clients.sec_client import SECClient; ..."
```

### 生产环境

**选项 1：云服务器 + Cron**
```bash
# 在服务器上设置 cron job
0 2 * * * cd /path/to/data-pipeline && python main.py --mode once --task sec
```

**选项 2：Docker 容器**
```dockerfile
FROM python:3.11
WORKDIR /app
COPY . .
RUN pip install -r requirements.txt
CMD ["python", "main.py", "--mode", "schedule"]
```

**选项 3：云函数（AWS Lambda / Azure Functions）**
- 每个数据源一个函数
- EventBridge/Timer 触发
- 轻量级，按需付费

## 📈 性能优化建议

1. **并发处理**
   - 使用多线程/多进程处理多个文件
   - 当前实现是串行的，可以改进

2. **增量更新**
   - 记录已处理的文件哈希
   - 避免重复处理

3. **缓存嵌入**
   - 对于未变化的文本，缓存嵌入结果
   - 减少 API 调用

4. **批量操作**
   - Pinecone upsert 使用批量操作（已实现）
   - 减少网络往返

## 🔐 安全注意事项

1. **API 密钥管理**
   - 使用环境变量
   - 不要提交 `.env` 文件到 Git

2. **频率限制**
   - 遵守各 API 的速率限制
   - 使用 `RateLimiter`

3. **爬虫礼仪**
   - 遵守 `robots.txt`
   - 使用合理的延迟
   - 设置准确的 User-Agent

4. **数据隐私**
   - 不采集敏感个人信息
   - 遵守数据使用条款

## 📝 扩展方向

### 短期（1-2周）
- [ ] 添加更多数据源（BCG, Deloitte）
- [ ] 实现 World Bank API 客户端
- [ ] 添加数据去重逻辑
- [ ] 改进行业分类准确性

### 中期（1个月）
- [ ] 实现增量更新机制
- [ ] 添加数据质量检查
- [ ] 实现并发处理
- [ ] 添加监控和告警

### 长期（3个月）
- [ ] 使用 Airflow 替换简单调度器
- [ ] 实现分布式爬虫
- [ ] 添加机器学习模型用于内容分类
- [ ] 构建数据管理界面

## 🤝 贡献指南

1. 遵循现有代码风格
2. 添加适当的日志记录
3. 编写测试用例
4. 更新文档

## 📄 许可证

本项目仅用于教育和研究目的。

---

**维护者**: 你的团队
**最后更新**: 2024

